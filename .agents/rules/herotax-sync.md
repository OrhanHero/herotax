# HeroTax Sync Directory Rule

- **Sync Directory Location**: `C:\Users\Hero\Documents\HeroTax`
- Whenever the user requests to rebuild into or update the sync folder:
  1. Execute `npm run build`
  2. Copy/mirror all contents from `dist/` directly into `C:\Users\Hero\Documents\HeroTax`.
